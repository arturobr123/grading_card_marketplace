//this file was just for testing

//we can only declare types in .ts (typescript) files

export interface Animal {
    legs: number;
}

export type cardObject ={
    firstName: String,
    lastName: String,
    jobTitle: String,
    type: String,
    avatarURL: String,
    status: String,
    lastLocation: String,
};